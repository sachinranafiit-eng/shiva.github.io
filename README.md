# Shiva Enterprises Website

Upload these files to a GitHub repository and enable GitHub Pages.

## Important edits before publishing
1. Replace `assets/logo.svg` with the official company logo file if you have it.
2. Replace all `+91-XXXXXXXXXX`, WhatsApp number, email, and map location.
3. Replace `https://example.com` in `index.html`, `robots.txt`, and `sitemap.xml` with your live domain.
4. Add real product images inside `assets/` and update product cards if desired.

This is a static website: no server, database, or online payment needed.
